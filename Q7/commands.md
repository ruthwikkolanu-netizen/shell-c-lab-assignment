Command:
./patterns.sh words.txt

Output:
Processing completed.

Explanation:
This command runs patterns.sh which reads words from words.txt, ignores case, and separates words into vowels.txt, consonants.txt, and mixed.txt based on vowel and consonant patterns.

Command:
cat vowels.txt

Output:
<paste output>

Explanation:
Displays words containing only vowels.
