Command:
./sync.sh dirA dirB

Output:
<paste the output here>

Explanation:
This command compares the contents of dirA and dirB. It lists files present only in each directory and uses cmp to check whether common files have matching or different contents.
