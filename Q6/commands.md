Command:
./metrics.sh input.txt

Output:
<paste your output here>

Explanation:
This command runs metrics.sh which reads input.txt, extracts words using tr, processes them using pipes, and calculates longest word, shortest word, average word length, and number of unique words.
